td
  = modelIndex + 1
td
  = customer.get('name') + " (" + customer.get('driverLicense') + ")"
td
  = payment.get('amount')
td
  = moment(takenOn).format(App.DataHelper.dateFormats.us)
td
  = returnedOn ? moment(returnedOn).format(App.DataHelper.dateFormats.us): ""
td
  span(class="#{status.toLowerCase()} item_status")
    = status
td
  a.btn.default.btn-xs.blue.show-rentals(href="#" data-toggle="tooltip" title="Rent Agreements of Deposit")
    i.fa.fa-file-text-o
  if status == 'ACTIVE'
    a.btn.default.btn-xs.green.return-deposit(href="#" data-toggle="tooltip" title="Return Deposit")
      i.fa.fa-reply
